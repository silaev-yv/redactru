-r requirements.txt
# Установите один из вариантов torch отдельно, подходящий под вашу CUDA/CPU.
# Примеры (выполняются отдельной командой, не через этот файл):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu129
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

transformers>=4.41
tokenizers>=0.15
accelerate>=0.31